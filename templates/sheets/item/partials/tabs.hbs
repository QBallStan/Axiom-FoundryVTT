<nav class="sheet-tabs tabs" data-group="sheet">
  {{#each tabs as |tab|}}
    <a class="item {{tab.cssClass}}" data-action="tab" data-group="sheet" data-tab="{{tab.id}}">
      <i class="{{tab.icon}}"></i>
      <span>{{localize tab.label}}</span>
    </a>
  {{/each}}
</nav>
